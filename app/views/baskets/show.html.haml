#basket
  .item_count
    %h2
      = pluralize current_basket.count, 'item'

- @products.each do |product|
  .product
    %h3
      .product-name
        = link_to product.name, product
      .price
        = "£#{product.price}"
      .description
        = product.description
      .remove_item_button
        = link_to "Remove from basket", delete_basket_products_path(product_id: product.id), method: :delete